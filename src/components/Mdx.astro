---
// src/components/MDX.astro
---
<div class="pros-contras-container">
    <div class="column pros">
        <h3><span class="icon">✨</span>Prós</h3>
        <slot name="pros" />
    </div>
    <div class="column contras">
        <h3><span class="icon">🤔</span>Contras</h3>
        <slot name="contras" />
    </div>
</div>

<style>
    .pros-contras-container {
        display: grid;
        grid-template-columns: 1fr;
        gap: 2rem;
        margin: 3rem 0;
    }

    @media (min-width: 768px) {
        .pros-contras-container {
            grid-template-columns: 1fr 1fr;
        }
    }

    .column {
        padding: 1.5rem;
        border-radius: 12px;
        background-color: var(--color-bg-light);
    }

    .column h3 {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        font-size: 1.5rem;
        margin-top: 0;
        padding-bottom: 1rem;
        border-bottom: 1px solid var(--color-bg-form);
    }

    .pros h3 {
        color: var(--accent-turquoise);
    }

    .contras h3 {
        color: var(--accent-lightpink);
    }
    
    .column :global(ul) {
        list-style: none;
        padding-left: 0;
    }

    .column :global(li) {
        margin-bottom: 1rem;
        padding-left: 1.5rem;
        position: relative;
    }

    .column :global(li::before) {
        position: absolute;
        left: 0;
        font-size: 1rem;
    }

    .pros :global(li::before) {
        content: '✓';
        color: var(--accent-green);
    }

    .contras :global(li::before) {
        content: '⚠️';
        color: var(--accent-red);
    }
</style>